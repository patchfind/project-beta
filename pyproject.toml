[project]
name = "project-beta-sample-rag"
version = "0.1.0"
description = "Minimal sample LangGraph/RAG project using local models and a FAISS vector store."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["app"]
